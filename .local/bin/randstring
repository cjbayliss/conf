#!/usr/bin/env python3

import secrets
import string

pool = string.ascii_letters + string.digits + "!@#$%^&*()_+-"


def randstring(pool, length):
    return "".join(secrets.choice(pool) for i in range(length))


def randomnumber():
    while True:
        x = secrets.randbelow(45)
        if x >= 15:
            return x
            break


for i in range(randomnumber()):
    print(randstring(pool, randomnumber()))
