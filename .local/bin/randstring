#!/usr/bin/env python3

import secrets
import string


def randstring(pool, length):
    return "".join(secrets.choice(pool) for i in range(length))


def randomnumber():
    while True:
        x = secrets.randbelow(45)
        if x >= 15:
            return x
            break


for i in range(randomnumber()):
    print(randstring(string.ascii_letters + "0123456789!@#$%^&*()_+", randomnumber()))
